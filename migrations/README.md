# Миграции БД

SQL-файлы выполняются по порядку имени. Уже применённые записываются в таблицу `__migrations`.

**Запуск на сервере или локально:**
```bash
npm run db:migrate
```

Требуется `DATABASE_URL` в `.env`.
