# Блок 2 — юридические страницы для сторов

- Публичная страница **Условия использования** (`/terms`), без авторизации.
- URL для консолей: `getTermsOfUseUrl()` в `client/src/lib/legal.ts` (`VITE_TERMS_OF_USE_URL` или `${base}/terms`).
- Файлы в папке — не длиннее ~200 строк; правки текста — по согласованию с продуктом/юристом.
