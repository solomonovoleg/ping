/**
 * Центр нижнего меню PULSE → полный **ПИНГОК МИКРО** (исходники: `ПИНГОК МИКРО/client/`).
 *
 * **Нельзя заменять на «тап в профиль»** — тогда пропадает весь продуктовый слой:
 * - удержание логотипа + кольцо → `PingokMicroOverlay`;
 * - **Web Speech API** (`usePingokMicroStt`): запись и режим стрима (свайп вверх при hold);
 * - `POST /api/pingok-micro/v1/parse` → интенты;
 * - **поиск в памяти** (AI memory), **лента по запросу** (`find` / `show`);
 * - **исполнение**: напоминания, план, задачи, **сообщение контакту**, **аудио/видео звонок**
 *   (`execute`, при необходимости `send-dm`, `start-call`, голосовые уточнения да/нет и выбор из списка).
 *
 * Реэкспорт сохраняет одну точку правды в `ПИНГОК МИКРО` и совпадает с алиасом `@pingok-micro` в Vite/tsconfig.
 */
export { NavPulseCenterLogoButton } from "@pingok-micro/NavPulseCenterLogoButton";
