# Скопируй в deploy.env и подставь свои значения.
# Запуск: source deploy/env.example или export переменные вручную, затем ./deploy/upload.sh
# Или: source deploy.env 2>/dev/null; ./deploy/upload.sh

export VPS_HOST=your-server.com
export VPS_USER=deploy
export VPS_PATH=/var/www/ping-moot
