node_modules
dist
.env
.env.local
.env.*.local
deploy.env
deploy.env.tmp
# Локальный staging с паролями — не коммитить (секреты в GitHub Actions)
deploy.staging.env
docs/CREDENTIALS.md
.DS_Store
server/public
uploads
vite.config.ts.*
*.tar.gz

# Android: путь к SDK у каждого свой
android/local.properties

# Локальные бэкапы и состояние
.env.bak
.local/