/**
 * Сколько последних комментариев по времени отдаём в карточку поста (лента, профиль, сохранённое).
 * В UI показывается только самый свежий; лимит 1 уменьшает полезную нагрузку API.
 * Сервер: `row_number … rn <= N`; клиент: тот же предел при оптимистичном обновлении.
 */
export const FEED_LATEST_COMMENTS_PREVIEW_LIMIT = 1;
