/** Максимальная длительность видео в посте после обрезки, сек. */
export const POST_VIDEO_MAX_SECONDS = 14;

/** Максимальная длительность видео в сториз после обрезки на сервере, сек. */
export const STORY_VIDEO_MAX_SECONDS = POST_VIDEO_MAX_SECONDS;

/** Максимум для «живого» аватара после обрезки на сервере, сек. */
export const AVATAR_VIDEO_MAX_SECONDS = 4;

/** Минимальная длина выбираемого фрагмента при ручной обрезке */
export const POST_VIDEO_MIN_SEGMENT_SECONDS = 0.5;
