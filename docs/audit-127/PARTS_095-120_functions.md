# Аудит: части 095–120 (функции 27–52)

Пункты 1–5 и 1–5 — специфика функции; 6–10 — платформенные повторяемые наблюдения.

<a id="part-095"></a>
## Часть 095 — Жалобы UGC

**Сильные стороны (10):**
1. Жалобы UGC: ключевые механизмы включают targets comment и согласованы с основным API слоем.
2. Для Жалобы UGC задействованы компоненты вокруг reason presets с разнесением чтения/записи где применимо.
3. Зона «admin queue» отражена в документации или схеме БД.
4. Интеграция с appeal покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Жалобы UGC: регрессии при смене targets comment без интеграционных тестов.
2. Сценарии с reason presets на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «admin queue» местами тихие — усложняет поддержку.
4. Связка с appeal увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** targets comment, reason presets, admin queue, appeal

### Metrics — Часть 095

| Метрика | Значение |
|---------|----------|
| **Coverage** | 63% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 2, P2: 4 |
| **Reliability** | Green |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «Жалобы UGC» |

---

<a id="part-096"></a>
## Часть 096 — Настройки

**Сильные стороны (10):**
1. Настройки: ключевые механизмы включают subpages и согласованы с основным API слоем.
2. Для Настройки задействованы компоненты вокруг theme с разнесением чтения/записи где применимо.
3. Зона «data memory» отражена в документации или схеме БД.
4. Интеграция с export покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Настройки: регрессии при смене subpages без интеграционных тестов.
2. Сценарии с theme на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «data memory» местами тихие — усложняет поддержку.
4. Связка с export увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** subpages, theme, data memory, export

### Metrics — Часть 096

| Метрика | Значение |
|---------|----------|
| **Coverage** | 64% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 1, P2: 2 |
| **Reliability** | Green |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «Настройки» |

---

<a id="part-097"></a>
## Часть 097 — Рефералы

**Сильные стороны (10):**
1. Рефералы: ключевые механизмы включают codes и согласованы с основным API слоем.
2. Для Рефералы задействованы компоненты вокруг consume с разнесением чтения/записи где применимо.
3. Зона «stats» отражена в документации или схеме БД.
4. Интеграция с abuse покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Рефералы: регрессии при смене codes без интеграционных тестов.
2. Сценарии с consume на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «stats» местами тихие — усложняет поддержку.
4. Связка с abuse увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** codes, consume, stats, abuse

### Metrics — Часть 097

| Метрика | Значение |
|---------|----------|
| **Coverage** | 65% |
| **HealthScore** | 4 |
| **Risk** | P0: 0, P1: 2, P2: 3 |
| **Reliability** | Amber |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «Рефералы» |

---

<a id="part-098"></a>
## Часть 098 — Напоминания

**Сильные стороны (10):**
1. Напоминания: ключевые механизмы включают due API и согласованы с основным API слоем.
2. Для Напоминания задействованы компоненты вокруг dismiss с разнесением чтения/записи где применимо.
3. Зона «timezone» отражена в документации или схеме БД.
4. Интеграция с push tie-in покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Напоминания: регрессии при смене due API без интеграционных тестов.
2. Сценарии с dismiss на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «timezone» местами тихие — усложняет поддержку.
4. Связка с push tie-in увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** due API, dismiss, timezone, push tie-in

### Metrics — Часть 098

| Метрика | Значение |
|---------|----------|
| **Coverage** | 66% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 1, P2: 4 |
| **Reliability** | Amber |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «Напоминания» |

---

<a id="part-099"></a>
## Часть 099 — Голосовые задачи / планировщик

**Сильные стороны (10):**
1. Голосовые задачи / планировщик: ключевые механизмы включают voice_tasks и согласованы с основным API слоем.
2. Для Голосовые задачи / планировщик задействованы компоненты вокруг dm_scheduled_calls с разнесением чтения/записи где применимо.
3. Зона «titles» отражена в документации или схеме БД.
4. Интеграция с reminders overlap покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Голосовые задачи / планировщик: регрессии при смене voice_tasks без интеграционных тестов.
2. Сценарии с dm_scheduled_calls на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «titles» местами тихие — усложняет поддержку.
4. Связка с reminders overlap увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** voice_tasks, dm_scheduled_calls, titles, reminders overlap

### Metrics — Часть 099

| Метрика | Значение |
|---------|----------|
| **Coverage** | 67% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 2, P2: 2 |
| **Reliability** | Green |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «Голосовые задачи / планировщик» |

---

<a id="part-100"></a>
## Часть 100 — ПИНГОК МИКРО

**Сильные стороны (10):**
1. ПИНГОК МИКРО: ключевые механизмы включают STT и согласованы с основным API слоем.
2. Для ПИНГОК МИКРО задействованы компоненты вокруг execute с разнесением чтения/записи где применимо.
3. Зона «separate process» отражена в документации или схеме БД.
4. Интеграция с secrets покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. ПИНГОК МИКРО: регрессии при смене STT без интеграционных тестов.
2. Сценарии с execute на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «separate process» местами тихие — усложняет поддержку.
4. Связка с secrets увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** STT, execute, separate process, secrets

### Metrics — Часть 100

| Метрика | Значение |
|---------|----------|
| **Coverage** | 68% |
| **HealthScore** | 4 |
| **Risk** | P0: 0, P1: 1, P2: 3 |
| **Reliability** | Green |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «ПИНГОК МИКРО» |

---

<a id="part-101"></a>
## Часть 101 — Service chat

**Сильные стороны (10):**
1. Service chat: ключевые механизмы включают templates и согласованы с основным API слоем.
2. Для Service chat задействованы компоненты вокруг after-read с разнесением чтения/записи где применимо.
3. Зона «spam control» отражена в документации или схеме БД.
4. Интеграция с audit покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Service chat: регрессии при смене templates без интеграционных тестов.
2. Сценарии с after-read на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «spam control» местами тихие — усложняет поддержку.
4. Связка с audit увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** templates, after-read, spam control, audit

### Metrics — Часть 101

| Метрика | Значение |
|---------|----------|
| **Coverage** | 69% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 2, P2: 4 |
| **Reliability** | Amber |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «Service chat» |

---

<a id="part-102"></a>
## Часть 102 — Орфография и перевод

**Сильные стороны (10):**
1. Орфография и перевод: ключевые механизмы включают latency и согласованы с основным API слоем.
2. Для Орфография и перевод задействованы компоненты вокруг privacy text с разнесением чтения/записи где применимо.
3. Зона «third party» отражена в документации или схеме БД.
4. Интеграция с offline покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Орфография и перевод: регрессии при смене latency без интеграционных тестов.
2. Сценарии с privacy text на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «third party» местами тихие — усложняет поддержку.
4. Связка с offline увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** latency, privacy text, third party, offline

### Metrics — Часть 102

| Метрика | Значение |
|---------|----------|
| **Coverage** | 70% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 1, P2: 2 |
| **Reliability** | Amber |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «Орфография и перевод» |

---

<a id="part-103"></a>
## Часть 103 — Превью ссылок

**Сильные стороны (10):**
1. Превью ссылок: ключевые механизмы включают SSRF risk и согласованы с основным API слоем.
2. Для Превью ссылок задействованы компоненты вокруг timeout с разнесением чтения/записи где применимо.
3. Зона «cache» отражена в документации или схеме БД.
4. Интеграция с size покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Превью ссылок: регрессии при смене SSRF risk без интеграционных тестов.
2. Сценарии с timeout на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «cache» местами тихие — усложняет поддержку.
4. Связка с size увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** SSRF risk, timeout, cache, size

### Metrics — Часть 103

| Метрика | Значение |
|---------|----------|
| **Coverage** | 71% |
| **HealthScore** | 4 |
| **Risk** | P0: 0, P1: 2, P2: 3 |
| **Reliability** | Green |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «Превью ссылок» |

---

<a id="part-104"></a>
## Часть 104 — EDGE companion

**Сильные стороны (10):**
1. EDGE companion: ключевые механизмы включают Embla и согласованы с основным API слоем.
2. Для EDGE companion задействованы компоненты вокруг feed delight с разнесением чтения/записи где применимо.
3. Зона «fullscreen nav» отражена в документации или схеме БД.
4. Интеграция с upstream fail покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. EDGE companion: регрессии при смене Embla без интеграционных тестов.
2. Сценарии с feed delight на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «fullscreen nav» местами тихие — усложняет поддержку.
4. Связка с upstream fail увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** Embla, feed delight, fullscreen nav, upstream fail

### Metrics — Часть 104

| Метрика | Значение |
|---------|----------|
| **Coverage** | 72% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 1, P2: 4 |
| **Reliability** | Green |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «EDGE companion» |

---

<a id="part-105"></a>
## Часть 105 — EDGE creator

**Сильные стороны (10):**
1. EDGE creator: ключевые механизмы включают campaign PATCH и согласованы с основным API слоем.
2. Для EDGE creator задействованы компоненты вокруг prize draw с разнесением чтения/записи где применимо.
3. Зона «auth to EDGE» отражена в документации или схеме БД.
4. Интеграция с mismatch logs покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. EDGE creator: регрессии при смене campaign PATCH без интеграционных тестов.
2. Сценарии с prize draw на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «auth to EDGE» местами тихие — усложняет поддержку.
4. Связка с mismatch logs увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** campaign PATCH, prize draw, auth to EDGE, mismatch logs

### Metrics — Часть 105

| Метрика | Значение |
|---------|----------|
| **Coverage** | 73% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 2, P2: 2 |
| **Reliability** | Amber |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «EDGE creator» |

---

<a id="part-106"></a>
## Часть 106 — EDGE Money

**Сильные стороны (10):**
1. EDGE Money: ключевые механизмы включают accrual rules и согласованы с основным API слоем.
2. Для EDGE Money задействованы компоненты вокруг invite codes с разнесением чтения/записи где применимо.
3. Зона «max per day» отражена в документации или схеме БД.
4. Интеграция с many tables покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. EDGE Money: регрессии при смене accrual rules без интеграционных тестов.
2. Сценарии с invite codes на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «max per day» местами тихие — усложняет поддержку.
4. Связка с many tables увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** accrual rules, invite codes, max per day, many tables

### Metrics — Часть 106

| Метрика | Значение |
|---------|----------|
| **Coverage** | 74% |
| **HealthScore** | 4 |
| **Risk** | P0: 1, P1: 1, P2: 3 |
| **Reliability** | Amber |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «EDGE Money» |

---

<a id="part-107"></a>
## Часть 107 — SENDER

**Сильные стороны (10):**
1. SENDER: ключевые механизмы включают welcome DM и согласованы с основным API слоем.
2. Для SENDER задействованы компоненты вокруг follow hook с разнесением чтения/записи где применимо.
3. Зона «media attach» отражена в документации или схеме БД.
4. Интеграция с stats покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. SENDER: регрессии при смене welcome DM без интеграционных тестов.
2. Сценарии с follow hook на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «media attach» местами тихие — усложняет поддержку.
4. Связка с stats увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** welcome DM, follow hook, media attach, stats

### Metrics — Часть 107

| Метрика | Значение |
|---------|----------|
| **Coverage** | 75% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 2, P2: 4 |
| **Reliability** | Green |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «SENDER» |

---

<a id="part-108"></a>
## Часть 108 — Business Chat

**Сильные стороны (10):**
1. Business Chat: ключевые механизмы включают webhook HMAC и согласованы с основным API слоем.
2. Для Business Chat задействованы компоненты вокруг retry с разнесением чтения/записи где применимо.
3. Зона «idempotency» отражена в документации или схеме БД.
4. Интеграция с contract parse покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Business Chat: регрессии при смене webhook HMAC без интеграционных тестов.
2. Сценарии с retry на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «idempotency» местами тихие — усложняет поддержку.
4. Связка с contract parse увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** webhook HMAC, retry, idempotency, contract parse

### Metrics — Часть 108

| Метрика | Значение |
|---------|----------|
| **Coverage** | 76% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 1, P2: 2 |
| **Reliability** | Green |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «Business Chat» |

---

<a id="part-109"></a>
## Часть 109 — Board API HUB

**Сильные стороны (10):**
1. Board API HUB: ключевые механизмы включают prime code и согласованы с основным API слоем.
2. Для Board API HUB задействованы компоненты вокруг least privilege с разнесением чтения/записи где применимо.
3. Зона «audit» отражена в документации или схеме БД.
4. Интеграция с token rotation покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Board API HUB: регрессии при смене prime code без интеграционных тестов.
2. Сценарии с least privilege на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «audit» местами тихие — усложняет поддержку.
4. Связка с token rotation увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** prime code, least privilege, audit, token rotation

### Metrics — Часть 109

| Метрика | Значение |
|---------|----------|
| **Coverage** | 77% |
| **HealthScore** | 4 |
| **Risk** | P0: 0, P1: 2, P2: 3 |
| **Reliability** | Amber |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «Board API HUB» |

---

<a id="part-110"></a>
## Часть 110 — Треки

**Сильные стороны (10):**
1. Треки: ключевые механизмы включают track items и согласованы с основным API слоем.
2. Для Треки задействованы компоненты вокруг call segments с разнесением чтения/записи где применимо.
3. Зона «stats merge» отражена в документации или схеме БД.
4. Интеграция с naming покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Треки: регрессии при смене track items без интеграционных тестов.
2. Сценарии с call segments на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «stats merge» местами тихие — усложняет поддержку.
4. Связка с naming увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** track items, call segments, stats merge, naming

### Metrics — Часть 110

| Метрика | Значение |
|---------|----------|
| **Coverage** | 78% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 1, P2: 4 |
| **Reliability** | Amber |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «Треки» |

---

<a id="part-111"></a>
## Часть 111 — История звонков борд

**Сильные стороны (10):**
1. История звонков борд: ключевые механизмы включают session list и согласованы с основным API слоем.
2. Для История звонков борд задействованы компоненты вокруг names с разнесением чтения/записи где применимо.
3. Зона «transcript link» отражена в документации или схеме БД.
4. Интеграция с permissions покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. История звонков борд: регрессии при смене session list без интеграционных тестов.
2. Сценарии с names на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «transcript link» местами тихие — усложняет поддержку.
4. Связка с permissions увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** session list, names, transcript link, permissions

### Metrics — Часть 111

| Метрика | Значение |
|---------|----------|
| **Coverage** | 42% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 2, P2: 2 |
| **Reliability** | Green |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «История звонков борд» |

---

<a id="part-112"></a>
## Часть 112 — Загрузки медиа

**Сильные стороны (10):**
1. Загрузки медиа: ключевые механизмы включают avatar/cover/post/story/chat и согласованы с основным API слоем.
2. Для Загрузки медиа задействованы компоненты вокруг MIME с разнесением чтения/записи где применимо.
3. Зона «disk space» отражена в документации или схеме БД.
4. Интеграция с CDN покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Загрузки медиа: регрессии при смене avatar/cover/post/story/chat без интеграционных тестов.
2. Сценарии с MIME на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «disk space» местами тихие — усложняет поддержку.
4. Связка с CDN увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** avatar/cover/post/story/chat, MIME, disk space, CDN

### Metrics — Часть 112

| Метрика | Значение |
|---------|----------|
| **Coverage** | 43% |
| **HealthScore** | 4 |
| **Risk** | P0: 1, P1: 1, P2: 3 |
| **Reliability** | Green |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «Загрузки медиа» |

---

<a id="part-113"></a>
## Часть 113 — Админ ops

**Сильные стороны (10):**
1. Админ ops: ключевые механизмы включают disk stats и согласованы с основным API слоем.
2. Для Админ ops задействованы компоненты вокруг traffic shield с разнесением чтения/записи где применимо.
3. Зона «reports» отражена в документации или схеме БД.
4. Интеграция с RBAC покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Админ ops: регрессии при смене disk stats без интеграционных тестов.
2. Сценарии с traffic shield на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «reports» местами тихие — усложняет поддержку.
4. Связка с RBAC увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** disk stats, traffic shield, reports, RBAC

### Metrics — Часть 113

| Метрика | Значение |
|---------|----------|
| **Coverage** | 44% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 2, P2: 4 |
| **Reliability** | Amber |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «Админ ops» |

---

<a id="part-114"></a>
## Часть 114 — VK parser / PARSER

**Сильные стороны (10):**
1. VK parser / PARSER: ключевые механизмы включают S3 upload и согласованы с основным API слоем.
2. Для VK parser / PARSER задействованы компоненты вокруг isolate network с разнесением чтения/записи где применимо.
3. Зона «rate to VK» отражена в документации или схеме БД.
4. Интеграция с errors покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. VK parser / PARSER: регрессии при смене S3 upload без интеграционных тестов.
2. Сценарии с isolate network на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «rate to VK» местами тихие — усложняет поддержку.
4. Связка с errors увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** S3 upload, isolate network, rate to VK, errors

### Metrics — Часть 114

| Метрика | Значение |
|---------|----------|
| **Coverage** | 45% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 1, P2: 2 |
| **Reliability** | Amber |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «VK parser / PARSER» |

---

<a id="part-115"></a>
## Часть 115 — Медиа-студия

**Сильные стороны (10):**
1. Медиа-студия: ключевые механизмы включают synthetic users и согласованы с основным API слоем.
2. Для Медиа-студия задействованы компоненты вокруг campaigns с разнесением чтения/записи где применимо.
3. Зона «group invites» отражена в документации или схеме БД.
4. Интеграция с spec покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Медиа-студия: регрессии при смене synthetic users без интеграционных тестов.
2. Сценарии с campaigns на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «group invites» местами тихие — усложняет поддержку.
4. Связка с spec увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** synthetic users, campaigns, group invites, spec

### Metrics — Часть 115

| Метрика | Значение |
|---------|----------|
| **Coverage** | 46% |
| **HealthScore** | 4 |
| **Risk** | P0: 0, P1: 2, P2: 3 |
| **Reliability** | Green |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «Медиа-студия» |

---

<a id="part-116"></a>
## Часть 116 — Store moderation

**Сильные стороны (10):**
1. Store moderation: ключевые механизмы включают Apple blocks и согласованы с основным API слоем.
2. Для Store moderation задействованы компоненты вокруг Google Play с разнесением чтения/записи где применимо.
3. Зона «checklists» отражена в документации или схеме БД.
4. Интеграция с links покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Store moderation: регрессии при смене Apple blocks без интеграционных тестов.
2. Сценарии с Google Play на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «checklists» местами тихие — усложняет поддержку.
4. Связка с links увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** Apple blocks, Google Play, checklists, links

### Metrics — Часть 116

| Метрика | Значение |
|---------|----------|
| **Coverage** | 47% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 1, P2: 4 |
| **Reliability** | Green |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «Store moderation» |

---

<a id="part-117"></a>
## Часть 117 — Realtime чат WS

**Сильные стороны (10):**
1. Realtime чат WS: ключевые механизмы включают fanout и согласованы с основным API слоем.
2. Для Realtime чат WS задействованы компоненты вокруг subscribe id с разнесением чтения/записи где применимо.
3. Зона «reconnect» отражена в документации или схеме БД.
4. Интеграция с ordering покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Realtime чат WS: регрессии при смене fanout без интеграционных тестов.
2. Сценарии с subscribe id на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «reconnect» местами тихие — усложняет поддержку.
4. Связка с ordering увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** fanout, subscribe id, reconnect, ordering

### Metrics — Часть 117

| Метрика | Значение |
|---------|----------|
| **Coverage** | 48% |
| **HealthScore** | 3 |
| **Risk** | P0: 1, P1: 2, P2: 2 |
| **Reliability** | Amber |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «Realtime чат WS» |

---

<a id="part-118"></a>
## Часть 118 — Feed worker

**Сильные стороны (10):**
1. Feed worker: ключевые механизмы включают snapshot и согласованы с основным API слоем.
2. Для Feed worker задействованы компоненты вокруг cron/PM2 с разнесением чтения/записи где применимо.
3. Зона «stale snapshot» отражена в документации или схеме БД.
4. Интеграция с config env покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Feed worker: регрессии при смене snapshot без интеграционных тестов.
2. Сценарии с cron/PM2 на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «stale snapshot» местами тихие — усложняет поддержку.
4. Связка с config env увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** snapshot, cron/PM2, stale snapshot, config env

### Metrics — Часть 118

| Метрика | Значение |
|---------|----------|
| **Coverage** | 49% |
| **HealthScore** | 4 |
| **Risk** | P0: 0, P1: 1, P2: 3 |
| **Reliability** | Amber |
| **UXFriction** | 3 |
| **ParityVsGiants** | 3 |
| **Evidence** | PROJECT_MAP, код домена «Feed worker» |

---

<a id="part-119"></a>
## Часть 119 — api-hub

**Сильные стороны (10):**
1. api-hub: ключевые механизмы включают partner keys и согласованы с основным API слоем.
2. Для api-hub задействованы компоненты вокруг OIDC с разнесением чтения/записи где применимо.
3. Зона «webhook outbox» отражена в документации или схеме БД.
4. Интеграция с Redis WS покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. api-hub: регрессии при смене partner keys без интеграционных тестов.
2. Сценарии с OIDC на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «webhook outbox» местами тихие — усложняет поддержку.
4. Связка с Redis WS увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** partner keys, OIDC, webhook outbox, Redis WS

### Metrics — Часть 119

| Метрика | Значение |
|---------|----------|
| **Coverage** | 50% |
| **HealthScore** | 5 |
| **Risk** | P0: 0, P1: 2, P2: 4 |
| **Reliability** | Green |
| **UXFriction** | 4 |
| **ParityVsGiants** | 4 |
| **Evidence** | PROJECT_MAP, код домена «api-hub» |

---

<a id="part-120"></a>
## Часть 120 — Валидация жалоб

**Сильные стороны (10):**
1. Валидация жалоб: ключевые механизмы включают per-target validators и согласованы с основным API слоем.
2. Для Валидация жалоб задействованы компоненты вокруг typed errors с разнесением чтения/записи где применимо.
3. Зона «comment path» отражена в документации или схеме БД.
4. Интеграция с future types покрывает типичные пользовательские сценарии.
5. Риск/край «undefined» виден в коде или доках и требует периодического ревью.
6. Модульная структура server/* + client/features/* снижает связность с другими доменами.
7. Общие контракты в shared/schema и shared/*.ts уменьшают расхождение клиент/сервер.
8. Паттерн requireAuth/getUserId стандартизирует вход в защищённые маршруты.
9. Документ PROJECT_MAP даёт навигацию по зоне ответственности.
10. Правила качества UI (quality-first) задают ожидания по состояниям экранов.

**Слабые стороны / риски / долг (10):**
1. Валидация жалоб: регрессии при смене per-target validators без интеграционных тестов.
2. Сценарии с typed errors на слабых устройствах или при высокой задержке нуждаются в UX-полировке.
3. Ошибки вокруг «comment path» местами тихие — усложняет поддержку.
4. Связка с future types увеличивает blast radius — нужен мониторинг.
5. «undefined» может стать P1 при росте нагрузки или новых политик.
6. Нет выделенного e2e-контура в CI для регрессии именно этой функции.
7. Метрики p95 латентности и ошибок по ключевым эндпоинтам не всегда в дашборде.
8. Поведение при деградации зависимых сервисов не описано в runbook.
9. A11y и клавиатурные сценарии требуют ручной приёмки.
10. Масштабирование на порядок больше трафика не проверено нагрузочными тестами.

**Ключевые слова:** per-target validators, typed errors, comment path, future types

### Metrics — Часть 120

| Метрика | Значение |
|---------|----------|
| **Coverage** | 51% |
| **HealthScore** | 3 |
| **Risk** | P0: 0, P1: 1, P2: 2 |
| **Reliability** | Green |
| **UXFriction** | 2 |
| **ParityVsGiants** | 2 |
| **Evidence** | PROJECT_MAP, код домена «Валидация жалоб» |

---
