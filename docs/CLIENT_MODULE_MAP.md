# Перенос: карта клиентских модулей

Карта **всего проекта** (включая клиентские `features/`, размеры файлов, правила изоляции) теперь в одном документе:

→ **`docs/PROJECT_MAP.md`** (раздел «Клиент» и «Правила модулей»)

Этот файл сохранён для старых ссылок. Новые строки в таблицы модулей добавляйте в `PROJECT_MAP.md`.
