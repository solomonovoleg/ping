# Перенос: актуальная архитектура

Вся структура проекта, таблицы модулей, потоки данных и **правило дописывать новые модули** собраны в одном файле:

→ **`docs/PROJECT_MAP.md`**

Этот файл оставлен, чтобы не ломать старые ссылки и закладки. Детали ниже дублировать не нужно — правьте только `PROJECT_MAP.md`.
