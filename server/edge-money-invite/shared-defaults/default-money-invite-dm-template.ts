/** Если в `money.inviteDm` нет шаблона — используем этот текст (плейсхолдеры как у companion). */
export const DEFAULT_MONEY_INVITE_DM_TEMPLATE = `Привет! Вот твои {{count}} персональных кода приглашения в PING по этой кампании (каждый код — для одного нового пользователя):

{{codes}}

Как пригласить: отправь другу один код — при регистрации в PING его вводят в поле приглашения.

Это личное сообщение от автора кампании — вкладка «Чаты» внизу экрана.`;
